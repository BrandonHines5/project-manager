import { ComingSoon } from "@/components/ui/coming-soon"

export default function DecisionsPage() {
  return (
    <ComingSoon
      feature="Decisions (Change Orders & Selections)"
      description="One page for every homeowner approval after contract — change orders and selections. Approvals will auto-create downstream to-dos."
    />
  )
}
